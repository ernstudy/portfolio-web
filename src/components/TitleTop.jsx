import { Box } from "@mui/material";
import React from "react";
import { ReactTyped } from "react-typed";

export default function TitleTop({ display }) {
  return (
    <Box component="div" className="title-top" sx={display}>
     
    </Box>
  );
}
